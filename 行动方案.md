# 小红书知识库 — 完整行动方案

> 目标：把小红书「点赞 + 收藏」的全部内容，采集到本地，自动分类整理，并提供工具台 + 前端界面进行浏览、搜索和管理。

---

## 0. 核心判断与关键思路（先看这里）

### 0.1 为什么不自己拼请求，而是"拦截响应"

小红书有很强的反爬（签名 `x-s` / `x-t` / `x-s-common`、设备指纹、风控、验证码）。如果从零构造请求，签名算法是 JS 混淆 + native 生成的，破解成本极高且随时失效。

**油猴脚本的正确用法不是"自己发请求"，而是"拦截网页自己发出的请求"。**

浏览器里你已经登录，页面本来就在调官方接口渲染数据。脚本只需要：

1. Hook `window.fetch` 和 `XMLHttpRequest`；
2. 当页面加载「收藏夹 / 点赞」列表时，把接口返回的 JSON 截下来；
3. 解析出每条笔记的结构化数据；
4. 存到本地（IndexedDB）或直接 POST 给本地后端。

这样**完全绕开签名问题**——因为请求是官方页面自己发的、自带合法签名，我们只做"旁路监听"。

### 0.2 一个必须提前知道的硬限制

小红书**「点赞」列表通常只能看到最近的一部分**（平台只保留近期点赞，历史点赞拿不全），而「收藏」可以按收藏夹翻页，但翻页也有上限。

→ 结论：**越早开始定期增量采集越好**，历史点赞很可能存在"缺口"，这是平台限制，不是脚本 bug。

### 0.3 需要你先拍板的 3 个决策点（影响架构）

| 决策点 | 推荐默认 | 备选 | 影响 |
|---|---|---|---|
| ① 运行位置 | 纯本地（localhost，单人使用） | 部署到服务器/公网 | 后端鉴权、成本、部署复杂度 |
| ② 分类引擎 | 云 LLM（Claude / 你现用的 provider） | 本地 Ollama / 纯规则 | 分类质量 vs 隐私 vs 成本 |
| ③ 媒体是否下载 | 下载到本地（图片/视频） | 只存 URL | 知识库耐久性 vs 磁盘占用 |

**已确认决策（2026-08-28）：**
- ① 运行位置 = **纯本地**（localhost，单人使用，不做登录鉴权）
- ② 分类引擎 = **云 LLM**（Claude，走 CC Switch 现有 provider；适配层保留可切 DeepSeek/GLM/Ollama）
- ③ 媒体 = **只存 URL**（不下载到本地，后端去掉下载队列；后续可选加"链接失效检测"）

---

## 1. 总体架构

```
┌─────────────────────────────────────────────────────────────┐
│  浏览器（已登录小红书）                                        │
│  ┌──────────────────────────────┐                            │
│  │  油猴脚本 (Tampermonkey)      │                            │
│  │  · Hook fetch/XHR 拦截响应    │                            │
│  │  · 自动滚动遍历 收藏夹/点赞    │                            │
│  │  · 解析笔记 JSON → 去重 → 提交 │                            │
│  └──────────────┬───────────────┘                            │
└─────────────────┼────────────────────────────────────────────┘
                  │ HTTP (localhost)
                  ▼
┌─────────────────────────────────────────────────────────────┐
│  后端服务 (Node.js + TypeScript + Fastify)                   │
│  · 接收笔记、去重入库                                          │
│  · 媒体下载队列（图片/视频落盘）                                │
│  · 调用 LLM 自动分类 + 打标签 + 摘要                            │
│  · 向量化 (embedding) + 语义检索                               │
│  · 任务调度（采集/分类/增量同步）                                │
└──────────────┬──────────────────────────────────────────────┘
               │
       ┌───────┴────────┐
       ▼                ▼
┌────────────┐   ┌─────────────────┐
│ SQLite      │   │ 媒体只存 URL     │
│ + sqlite-vec│   │ (images/video   │
│ (向量搜索)   │   │  字段存原始链接) │
└────────────┘   └─────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│  前端工具台 (React + Vite + TypeScript)                       │
│  · 概览统计 / 分类树筛选 / 关键词+语义搜索                      │
│  · 笔记卡片流 + 详情页（图文 / 视频播放）                       │
│  · 工具台：采集控制、分类任务状态、去重/清理、导出                │
└─────────────────────────────────────────────────────────────┘
```

数据流一句话总结：

> **浏览/自动采集 → 油猴拦截官方接口响应 → 解析 → 后端入库 → LLM 分类打标 → 向量化 → 前端检索浏览**

---

## 2. 技术栈选型

| 层 | 选型 | 理由 |
|---|---|---|
| 采集 | Tampermonkey 油猴脚本（原生 JS + 构建） | 运行在你的登录态里，拦截官方请求 |
| 后端 | Node.js + TypeScript + Fastify | 与前端/脚本同语言，统一心智 |
| 数据库 | SQLite（better-sqlite3 或 Drizzle） | 单文件零配置，个人知识库最合适 |
| 向量检索 | sqlite-vec（SQLite 扩展） | 不引入独立向量库，随库迁移 |
| 媒体存储 | 本地目录 `media/` + DB 存路径 | 图片/视频落盘，链接失效也能看 |
| LLM 分类 | 可插拔 provider 适配层（Claude / DeepSeek / GLM / Ollama） | 隐私与成本可切换 |
| 前端 | React + Vite + TypeScript | 生态成熟，组件化开发快 |

> 若你更熟 Python，后端可换 FastAPI（`sentence-transformers` 本地 embedding 更顺）；前端不变。核心架构不受影响。

---

## 3. 数据模型（核心表）

```sql
notes (
  id            TEXT PRIMARY KEY,   -- 小红书 note_id
  xsec_token    TEXT,               -- 拼详情页 URL 用
  title         TEXT,
  desc          TEXT,               -- 正文描述（LLM 分类的主要输入）
  type          TEXT,               -- normal(图文) | video
  author_id     TEXT,
  author_name   TEXT,
  author_avatar TEXT,
  source        TEXT,               -- liked | collect:<收藏夹名>
  collected_at  TEXT,               -- 采集时间
  note_time     TEXT,               -- 发布时间
  like_count    INTEGER,
  collect_count INTEGER,
  comment_count INTEGER,
  images        TEXT,               -- JSON 数组（图片 URL 列表）
  video_url     TEXT,
  raw           TEXT,               -- 原始 JSON 备份（可追溯）
  status        TEXT                -- pending_download | ready | media_missing | dead
);

media (
  id         TEXT PRIMARY KEY,
  note_id    TEXT REFERENCES notes(id),
  url        TEXT,
  kind       TEXT,                  -- image | video
  local_path TEXT,
  hash       TEXT,                  -- 内容哈希去重
  status     TEXT                   -- pending | downloaded | failed
);

categories (id INTEGER PK, name TEXT, parent_id INTEGER);       -- 分类树
note_categories (note_id, category_id);

tags (id INTEGER PK, name TEXT);
note_tags (note_id, tag_id);

embeddings (note_id, vector BLOB, model TEXT, dim INTEGER);     -- 语义检索

tasks (id, type, status, progress, created_at, finished_at);    -- 采集/分类任务
```

---

## 4. 分阶段实施计划

### 阶段 0 — 调研与定案（约 1–2 天）

**目标：** 确认可行性，锁定接口与范围。

1. 用浏览器打开小红书网页版 → 你的「收藏 / 点赞」页面 → F12 → Network 面板，确认：
   - 收藏/点赞列表对应的 API 请求路径（如 `.../user/collect`、`.../user/liked` 之类）；
   - 翻页方式（`cursor` / `page` 参数）；
   - 返回 JSON 里笔记字段结构。
2. 统计你的数据规模（点赞多少、收藏夹多少个、每夹多少条）。
3. 确认 LLM API 可用（Claude 走你现在的 provider，还是换本地 Ollama）。
4. 确认 3 个决策点（见 0.3）。

**交付物：** 一份接口清单 + 样本 JSON + 决策记录。

---

### 阶段 1 — 油猴抓取脚本（约 2–4 天）

**目标：** 能稳定抓取全部点赞/收藏的结构化数据。

1. **响应拦截层**：Hook `fetch` / `XHR`，识别收藏/点赞相关接口，序列化响应 JSON。
2. **主动采集层**：注入控制面板（一个悬浮按钮），一键启动后自动滚动页面触发翻页，同时拦截数据。两种模式：
   - 被动模式：你正常浏览时，后台静默采集；
   - 主动模式：脚本自动遍历每个收藏夹/点赞列表（控制频率 + 随机间隔）。
3. **数据落地**：先去重（按 note_id），再存入 IndexedDB，最后批量 POST 到本地后端（或先导出 JSON 文件再导入）。
4. **去重与增量**：记录已抓 note_id 集合，重复跑只取新增。

**交付物：** Tampermonkey 脚本 + 采集面板；能采到完整字段（含图片/视频 URL）。

---

### 阶段 2 — 后端数据服务（约 3–5 天）

**目标：** 数据入库、媒体落盘、可查询。

1. 初始化 Fastify 项目 + SQLite schema（上面的表）。
2. REST API：
   - `POST /notes`（批量接收，去重入库）
   - `GET /notes`（分页 / 筛选 / 关键词搜索）
   - `GET /notes/:id`（详情）
   - `GET /media/...`（本地媒体静态服务）
3. ~~媒体下载队列~~（已确认只存 URL，跳过；后续可选加"链接失效检测"）
4. 向量化接口：调用 embedding，写入 `embeddings` 表 + sqlite-vec 索引。

**交付物：** 可运行后端 + 一条笔记从采集到可查询/可看图的完整链路。

---

### 阶段 3 — 自动分类整理（约 2–4 天）

**目标：** 每条笔记自动归类、打标签、生成摘要。

1. **规则兜底**（先做，0 成本）：关键词 → 粗分类，给没有 LLM 时的默认结果。
2. **LLM 分类器**（核心）：
   - 输入：`title + desc`（可加作者/收藏夹名作为弱信号）；
   - 输出：`category（多级） + tags[] + 一句话摘要`，结构化 JSON；
   - 用 provider 适配层，可切 Claude / DeepSeek / GLM / Ollama。
3. **批量流水线**：队列 + 限速 + 断点续跑 + 失败重试 + 人工校正入口（分类错了可手改）。
4. **可选：聚类发现新类别**：对全部笔记 embedding 做聚类，发现规则没覆盖的主题，辅助完善分类树。
5. **默认分类树**（可人工改）：美食 / 穿搭 / 美妆 / 旅行 / 家居 / 学习 / 健身 / 摄影 / 情感 / 搞笑 / 科技 / 其他。

**交付物：** 全量笔记完成分类；分类树 + 标签 + 摘要可展示。

---

### 阶段 4 — 前端工具台（约 3–5 天）

**目标：** 可视化浏览 + 管理。

1. 仪表盘：总笔记数、分类分布、采集/分类任务状态、媒体完整性。
2. 浏览页：
   - 分类树 / 标签侧边筛选；
   - 关键词搜索 + 语义搜索（embedding 相似度）；
   - 时间线、瀑布流卡片。
3. 详情页：图文轮播 / 视频播放、摘要、标签、来源收藏夹、原文跳转链接。
4. 工具台（管理能力）：
   - 采集任务控制（启动/停止/增量同步）；
   - 分类任务状态与人工校正；
   - 去重、清理失效媒体、数据导出（JSON / Markdown）。

**交付物：** 一个能日常使用的本地 Web 工具台。

---

### 阶段 5 — 增量同步与维护（持续）

1. 定时增量采集（新点赞/新收藏自动入库）。
2. 失效链接检测（媒体 404 / 笔记被删的标记）。
3. 定期备份（SQLite + media 目录）。
4. 分类树迭代（新内容归入新分类）。

---

## 5. 风险与合规

| 风险 | 说明 | 应对 |
|---|---|---|
| 反爬/风控 | 高频操作可能触发验证码或临时限流 | 只采集自己账号数据；低频 + 随机间隔；优先被动拦截 |
| 点赞历史拿不全 | 平台只保留近期点赞 | 尽早开始定期增量采集，接受历史缺口 |
| 接口随时变 | 官方接口字段/路径会改 | 阶段 0 留接口清单；解析层做容错 + 可快速更新 |
| 账号安全 | 脚本运行在自己浏览器、自己登录态 | 不共享、不公开采集到的他人内容 |
| 版权 | 内容是他人原创 | 仅个人本地归档使用，不传播、不商用 |

---

## 6. 里程碑与交付物总览

| 里程碑 | 完成标志 |
|---|---|
| M1 调研定案 | 接口清单 + 样本 JSON + 决策记录 |
| M2 采集打通 | 油猴脚本能抓全点赞/收藏结构化数据 |
| M3 数据入库 | 后端跑通"采集→入库→下载媒体→可查询" |
| M4 自动分类 | 全量笔记完成分类 + 标签 + 摘要 |
| M5 工具台上线 | 前端可浏览/搜索/管理 |
| M6 增量同步 | 新内容自动入库，可持续使用 |

---

## 7. 建议的下一步

1. 先回答 **0.3 的 3 个决策点**（运行位置 / 分类引擎 / 是否下载媒体）；
2. 我们进入 **阶段 0**：我陪你用 DevTools 确认当前可用的接口和字段结构；
3. 确认后，我从 **阶段 1 油猴脚本** 开始动手写代码。
